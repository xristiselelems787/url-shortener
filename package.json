{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "URL Shortener with custom aliases",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "dependencies": {
    "nuxt": "^3.15.0",
    "nanoid": "^5.0.9"
  }
}
